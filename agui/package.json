{
  "name": "agui-poc",
  "version": "1.0.0",
  "private": true,
  "description": "AG-UI React chat app backed by a Google ADK agent",
  "scripts": {
    "dev": "concurrently -n agent,runtime,frontend -c cyan,yellow,green \"npm run dev:agent\" \"npm run dev:runtime\" \"npm run dev:frontend\"",
    "dev:agent": "cd agent && python main.py",
    "dev:agent:adk": "cd agent && adk api_server --port 8080",
    "dev:runtime": "cd runtime && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm run install:runtime && npm run install:frontend",
    "install:runtime": "cd runtime && npm install",
    "install:frontend": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
